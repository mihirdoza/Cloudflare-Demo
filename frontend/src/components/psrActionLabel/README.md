# React + TypeScript + Vite
actionLabel Component

used to render label and image(optional) inside div with onClick actions.
sub components used in it 
- label
- image

* do include Image and Label component before using this 

* in ActionLabel.tsx
# import Label from "../label/Label"; //have included Label component using relative path
# import Image from "../image/Image"; //have included Image component using relative path