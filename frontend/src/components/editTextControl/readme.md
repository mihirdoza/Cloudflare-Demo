# Simple edit text control with use of MUI TextField

## Packages used for the component 
# npm i @mui/material @emotion/react @emotion/styled

# refTable.json is used to get refTable data if input mask is provided 